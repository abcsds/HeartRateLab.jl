# References

Every entry in the [HRV Variable Zoo](index.md) is tied to the **seminal source**
that first introduced the measure (not a review that merely uses it). The
bibliography below is generated from `docs/references.bib`; each `[key](@cite)`
link in a dex entry resolves here. Metadata (authors, year, venue, DOI) was
verified against PubMed / publisher pages / Semantic Scholar in 2026-07.

```@bibliography
```
